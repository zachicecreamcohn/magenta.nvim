{
  "container": {
    "dockerfile": "docker/Dockerfile",
    "workspacePath": "/workspace"
  },
  "commandConfig": {
    "commands": [
      ["npx", "tsc", "--noEmit"],
      ["npx", "tsgo", { "type": "restAny" }],
      ["npx", "vitest", "run", { "type": "restAny" }]
    ]
  }
}
